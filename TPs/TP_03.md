---
jupyter:
  jupytext:
    cell_metadata_filter: -all
    formats: ipynb,md
    text_representation:
      extension: .md
      format_name: markdown
      format_version: '1.3'
      jupytext_version: 1.19.5
  kernelspec:
    display_name: Python (uv-env)
    language: python
    name: obsidian-uv
---


Hola que tal

Todo ok?

Si todo ok...


```python
bool("False")
```

```python
print(repr(True), str(True), True)
True , repr(True)
```

```python
a = bool(1)
print(a, repr(a), str(a))
a
```

```python
import numpy as np

a = np.array([1,2,3])
print(repr(a), str(a), a)
a
```

```python
import this

```

```python
a = 'cmd for other "program" '
b = "cmd for other \"program\" "
c = ("esto es un string"
     "y esto creo que sigue así"
    )
    
def f(x: int) -> int:
	return x+1
	
""" my fn hace tal cosa

    x es un entero
"""
print(a)
print(b)
print (c)
```




```python
fname = 'SAZS_2020_2026.csv'
with open(fname, 'rt') as f:
	data =f.read()

data_lines = data.split('\n')

#temp = []
#for row in range(1, len(data_lines)):
#    temp.append(data_lines[row].split(',')[6])

#temp = []
#for line in data_lines[1:]:
#    temp.append(line.split(',')[6])

times = [ line.split(',')[2] for line in data_lines[1:] ]

```



```python
for i, item in enumerate(temp):
	if i>10:
		break
	print(item)
```

# Fijate cómo anda esto

```python
print (times)
```

```python
help(plt)
```

```python
1.0 + 1.0 == 2.0
```
