# TP 01.2: Ejercicios

## 1) Text Slicing

Este es el comienzo de la novela _Asfixia_ de __Chuck Palahniuk__.

```python
texto = """Si vas a leer esto, no te preocupes. Al cabo de un par de páginas ya no querrás estar aquí. Así que olvídalo. Aléjate. Lárgate mientras sigas entero. Sálvate. Seguro que hay algo mejor en la televisión. O, ya que tienes tanto tiempo libre, a lo mejor puedes hacer un cursillo nocturno. Hazte médico. Puedes hacer algo útil con tu vida. Llévate a ti mismo a cenar. Tíñete el pelo. No te vas a volver más joven. Al principio lo que se cuenta aquí te va a hacer enojar. Luego se volverá cada vez peor.
"""
```

1) hacer una lista de strings donde cada elemento de la lista sea una oración, sin cortes de línea.
2) reescribir el texto ordenando las oraciones según su tamaño, de más cortas a más largas.
3) hacer una lista de palabras que aparecen en el texto
4) qué palabras se repiten? 
5) reescribir el texto original, pero con cada palabra escrita de atrás para adelante
6) reescribir el texto reemplazando las palabras de la siguiente manera: 
		- si una palabra tiene 5 letras o menos, no se modifica
		- en caso contrario, la palabra se rearma dejando las dos letras de cada extremo en su lugar, y la parte central de la palabra se escribe invertida
7) cuál es la letra más repetida del texto? y la que menos veces aparece? Hay alguna letra del alfabeto que no esté presente?

## 2) Comprehension

Arme una lista con todos los números de dos cifras que la suma de sus cifras sea 3. 
- verifique que son todos múltiplos de 3.
- haga un slicing para quedarse sólo con los múltiplos de 15.

## 3) Detector de pulsos

Considere la lista

```python
pulso = [0,0,0, 5,8,10,8,5, 0,0,0]
```

identifique los índices en los que comienza y termina el pulso.
```python
pulso = [0,0,0, 5,8,10,8,5, 0,0,0]
```

## 4) Mutantes, zombies y cabezaduras

En el siguiente código:

```python
lista_a = [10, 20, 30] 
lista_b = lista_a  
lista_b.append(40)

print(lista_a, lista_b)
```

- qué imprime el resultado? (Piénselo antes de correrlo)
- si quiero que se comporte diferente, qué tengo que hacer? (varias soluciones)

Ahora miren esta asignación:

```python
m = (10, 20)
```

- de qué mqnera puedo hacer que _m_ valga _(1, 20)_ sin escribir nunca el número 20 ?
(se premiará la solución más breve)

## 5) Repaso de ideas

Este código da una secuencia de números al azar

```python
import random 

numeros = list(range(1, 1001)) # 1 al 1000 
random.shuffle(numeros) # ahora están desordenados
numeros = numeros[:300] # 300 números al azar entre 1 y 1000
```

1) usando comprehension, haga una lista con los números pares
2) está 222 en la lista?
3) divida la lista en dos mitades de largo aproximado
4) vuelva a juntarlas y verifique que quedaron como antes. 

---

